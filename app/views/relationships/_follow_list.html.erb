<% if users.exists? %>
  <% users.each do |user| %>
    <table calss="table" width="80%">
        <tr>
          <%= link_to user_path(user), class: "nav-link" do %>
            <td width="20%"><%= image_tag user.get_profile_image, class:"rounded-circle", size: "50x50" %></td>
            <td width="50%"><%= user.name %></td>
          <% end %>
            <td><%= render "users/follow", user: user %></td>
        </tr>
    </table>
  <% end %>
<% else %>
  <p>ユーザーはいません</p>
<% end %>